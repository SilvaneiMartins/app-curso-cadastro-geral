import React from 'react';
import { Text } from 'react-native';

import { Container, Title } from './styles';

export const Header = () => {
    return (
        <Container>
            <Title>Header</Title>
        </Container>
    )
}
